name: Call a reusable workflow

on:
 workflow_dispatch:

jobs:
  call-workflow:
    uses: Shanridhu/github-actions-shared-workflow/.github/workflows/workflow1.yml@main

  #call-workflow-passing-data:
   # permissions:
    #  contents: read
     # pull-requests: write
    #uses: Shanridhu/github-actions-shared-workflow/.github/workflows/workflow1.yml@main
    
    
    secrets:
      token: ${{ secrets.Workflow2_PAT_TOKEN_GITHUB }}
